
let fib(n: Int): Int {
  var a = 0;
  var b = 1;
  while (n > 0) {
    var tmp = a + b;
    a = b;
    b = tmp;
    n -= 1;
  }
  return a;
}

for (var i = 0; i <= 45; i++) {
  console.log("fib(${i.toString()}) = ${fib(i).toString()}");
}
